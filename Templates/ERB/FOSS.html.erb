<!DOCTYPE html>
<html>
<head>
      <meta charset="utf-8">
      <meta name='viewport' content='width=device-width, initial-scale=1.0, minimum-scale=1.0'>
      <link rel="stylesheet" href="../styles.css">
</head>
<body>
      <h1>Free and OpenSource Software</h1>

      <h2>Inhalt</h2>

      <ul>
      <% dependencies.each do |dependency| %>
            <li><a href="#<%= dependency[:name] %>"><%= dependency[:name] %> <%= dependency[:version] %></a></li>
      <% end %>

      </ul>

      <h2>Lizenzen</h2>


      <% dependencies.each do |dependency| %>

      <h4 id="<%= dependency[:name] %>"><%= dependency[:name] %> <%= dependency[:version] %></h4>
      <p><a href="<%= dependency[:url] %>"><%= dependency[:url] %></a></p>
      <section class="license"><%= dependency[:license] %></section>

      <% end %>

</body>
