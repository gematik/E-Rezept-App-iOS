# Requirements<% SORTED_SPECS.each do |spec_name, afos| %>

## <%= spec_name %><% afos.each do |afo_name, details| %>

### <%= afo_name %>


<% details.each do |details| %>

#### <%= "#{details['part']} " if details['part'] %>`<%= details['file'] %>:<%= details['line'] %>`: <%= details['description'] %>

<%= details['source'] %>

```
<%= details['code'] %>
```

<% end # afos details %>

<% end # afos %>
<% end # specs %>
